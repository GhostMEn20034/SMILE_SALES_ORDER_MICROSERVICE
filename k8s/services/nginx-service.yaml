apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: smile-sales-orders
  labels:
    app: nginx-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30005
  selector:
    app: nginx